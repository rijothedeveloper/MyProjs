import { Link } from "react-router-dom";

const UseContext = () => {
  return (
    <div>
      <Link to="/contextHome">Context Home </Link>
      <Link to="/profile">profile </Link>
      <Link to="/contact">Contact </Link>
    </div>
  );
};

export default UseContext;
